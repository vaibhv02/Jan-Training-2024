import React from "react";

const AboutUs = () => {
    // const [err,setErr] = useState({})

    try{
        console.log(a);
    }

    catch(err){
        console.log(err,"error");

    }
    
    return(
        <div>
            <span>{}</span>
        </div>
    )
        
    
}

export default AboutUs;
